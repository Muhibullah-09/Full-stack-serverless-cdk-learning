type Todo {
  id: ID!
  title: String!
  done: Boolean!
}

input TodoInput {
  id: ID!
  title: String!
  done: Boolean!
}

type Query {
  getTodos: [Todo!]
}

type Mutation {
  addTodo(todo: TodoInput!): Todo
  updateTodo(todoDone: Boolean!): Boolean
  deleteTodo(todoId: String!): String
}